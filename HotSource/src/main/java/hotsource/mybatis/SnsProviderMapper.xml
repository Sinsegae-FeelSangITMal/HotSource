<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SnsProvider">
	
	<select id="selectByName" parameterType="String" resultType="SnsProvider">
		SELECT sns_provider_id, provider_name
		FROM sns_provider
		WHERE provider_name = #{provider_name}
	</select>
	
	<select id="select" parameterType="long" resultType="SnsProvider">
		SELECT sns_provider_id, provider_name
		FROM sns_provider
		WHERE sns_provider_id = #{sns_provider_id}
	</select>
	
</mapper>